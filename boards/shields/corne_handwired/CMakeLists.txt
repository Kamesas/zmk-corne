# Built-in status screen — no custom sources needed. If we revisit vertical
# rotation later, re-add custom_status_screen.c and widget sources here,
# guarded on CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM.
